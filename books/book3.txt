ZooKeeper provides a hierarchical namespace of data registers called znodes.
Ephemeral znodes exist as long as the session that created the znode is active.
Service discovery can be implemented by watching the children of a registry path.
